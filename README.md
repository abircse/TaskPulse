# Minimal Tracker

An ultra-lean, crash-proof Flutter tracker app that caches tracking timestamps using `shared_preferences` and performs low-power radius monitoring via `geolocator`.

## How to run
1. Extract this zip file.
2. Open terminal in the directory.
3. Run `flutter pub get`.
4. Run `flutter run`.
